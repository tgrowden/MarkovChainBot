'use strict'

const config = {
    team: {
        name: 'Markov Chain Bot', // used for logging
        token: 'yourtoken',
        connection: 'mongodb://localhost/markovChainBot'
    }
}

module.exports = config
